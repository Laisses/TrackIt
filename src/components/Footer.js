import styled from "styled-components"

export const Footer = () => {
    return (
        <Div>Eu sou o Footer</Div>
    );
};

const Div = styled.div`
    background-color: green;
    height: 10px;
`;

